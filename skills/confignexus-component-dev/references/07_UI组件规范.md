# 07 UI 组件规范

> **版本**: 2.0  
> **更新日期**: 2024-12  
> **参考应用**: `task_app`、`bag_app`、`mail_app`

本文档定义了 ConfigNexus 组件的统一 UI 组件与样式指南，基于三个生产应用的实际实现提取的规范。

---

## 🎨 设计系统：Cyberpunk 风格

### 1. 颜色系统

```css
/* 主色调 - 在 index.css 中定义 */
:root {
    /* 核心颜色 */
    --cp-red: #ff2a2a;           /* 主强调色 */
    --cp-red-dark: #880000;      /* 深红色 */
    --cp-red-dim: rgba(255, 42, 42, 0.1);
    
    /* 背景层次 */
    --cp-black: #050505;         /* 最深背景 */
    --cp-dark: #0f0f0f;          /* 深色背景 */
    --cp-bg: #121212;            /* 表面背景 */
    
    /* 文本颜色 */
    --cp-white: #f0f0f0;         /* 主文本 */
    --cp-gray: #333333;          /* 边框/分隔线 */
    --cp-gray-light: #666666;    /* 次要文本 */
    
    /* 功能颜色 */
    --cp-cyan: #00f0ff;          /* 信息色 */
    --cp-success: #39ff14;       /* 成功色 */
    --cp-warning: #f59e0b;       /* 警告色 */
}

/* 稀有度颜色 (用于物品卡片) */
@theme {
    --color-rarity-1: #39ff14;   /* 普通 - 绿色 */
    --color-rarity-2: #00f0ff;   /* 稀有 - 青色 */
    --color-rarity-3: #b026ff;   /* 史诗 - 紫色 */
    --color-rarity-4: #ff5e00;   /* 传说 - 橙色 */
    --color-rarity-5: #ffd700;   /* 神话 - 金色 */
}
```

### 2. 字体系统

```css
/* 推荐字体组合 */
body {
    font-family: 'Inter', 'Segoe UI', 'Noto Sans SC', sans-serif;
}

/* 等宽字体 - 用于代码/ID显示 */
.font-mono {
    font-family: 'JetBrains Mono', 'Share Tech Mono', 'Consolas', monospace;
}

/* 展示字体 - 用于标题 */
.font-display {
    font-family: 'Teko', 'Rajdhani', sans-serif;
}
```

### 3. 切角样式 (Clip Path)

```css
/* 标准切角 - 8px */
.clip-tech-chamfer {
    clip-path: polygon(8px 0, 100% 0, 100% calc(100% - 8px), calc(100% - 8px) 100%, 0 100%, 0 8px);
}

/* 大切角 - 10px */
.clip-chamfer {
    clip-path: polygon(10px 0, 100% 0, 100% calc(100% - 10px), calc(100% - 10px) 100%, 0 100%, 0 10px);
}

/* 角度切角 */
.clip-angled {
    clip-path: polygon(0 0, calc(100% - 20px) 0, 100% 20px, 100% 100%, 20px 100%, 0 calc(100% - 20px));
}
```

---

## 📐 顶部栏规范 (TopBar)

### 结构模板

所有应用均采用以下顶部栏结构：

```
┌─────────────────────────────────────────────────────────────────────────┐
│ ■ SYS.CONFIG.V.8.0 ─────────── ■ CYBER CODE SYSTEM ■ ─────── SECURE ■  │  装饰条
├─────────────────────────────────────────────────────────────────────────┤
│ [← 返回]  标题  [LIVE]           [📊] [🔗] [⚙️] [👤]                    │  主工具栏
│           副标题/状态                                                    │
└─────────────────────────────────────────────────────────────────────────┘
```

### 完整代码模板

```tsx
import React from 'react';
import { ArrowLeft, Link, Settings, User, FileText } from 'lucide-react';

interface TopBarProps {
  title: string;              // 应用标题
  subtitle?: string;          // 副标题/状态
  onBack?: () => void;        // 返回按钮回调
  onMapping?: () => void;     // 映射按钮回调
  onSettings?: () => void;    // 设置按钮回调
  onHelp?: () => void;        // 帮助按钮回调
  isConfigured?: boolean;     // 映射是否已配置
}

export const TopBar: React.FC<TopBarProps> = ({
  title,
  subtitle,
  onBack,
  onMapping,
  onSettings,
  onHelp,
  isConfigured = false,
}) => {
  // 切角样式
  const chamferClip = {
    clipPath: 'polygon(8px 0, 100% 0, 100% calc(100% - 8px), calc(100% - 8px) 100%, 0 100%, 0 8px)'
  };

  return (
    <div className="w-full flex flex-col bg-black border-b border-white/5">
      
      {/* 装饰条 */}
      <div className="h-6 flex items-center w-full px-4 overflow-hidden relative">
        <div className="absolute inset-x-0 top-1/2 h-[1px] bg-white/10 -z-10" />
        
        {/* 左侧文本 */}
        <div className="hidden sm:flex items-center gap-2 bg-black pr-4">
          <div className="w-2 h-2 bg-[#ff2a2a]" />
          <span className="text-[10px] font-mono text-gray-600 tracking-widest">SYS.CONFIG.V.8.0</span>
        </div>

        {/* 中央标识 */}
        <div className="mx-auto flex items-center gap-2 bg-black px-4">
          <div className="w-1.5 h-1.5 bg-[#ff2a2a]" />
          <div className="bg-[#1a0505] px-2 py-0.5">
            <span className="text-[10px] font-bold tracking-[0.2em] text-[#ff2a2a] uppercase">
              CYBER CODE SYSTEM
            </span>
          </div>
          <div className="w-1.5 h-1.5 bg-[#ff2a2a]" />
        </div>

        {/* 右侧状态 */}
        <div className="hidden sm:flex items-center gap-2 bg-black pl-4">
          <span className="text-[10px] font-mono text-gray-600 tracking-widest">
            {isConfigured ? 'SYNCED' : 'STANDBY'}
          </span>
          <div className={`w-2 h-2 ${isConfigured ? 'bg-green-500 animate-pulse' : 'bg-[#ff2a2a]'}`} />
        </div>
      </div>

      {/* 主工具栏 */}
      <div className="h-16 flex items-center justify-between px-4 sm:px-8">
        
        {/* 左侧：返回按钮 + 标题 */}
        <div className="flex items-center gap-4 sm:gap-6">
          {onBack && (
            <button 
              onClick={onBack}
              className="h-10 px-4 sm:px-6 bg-[#0a0a0a] border border-[#222] text-white hover:bg-[#151515] hover:border-[#ff2a2a]/50 transition-all flex items-center gap-2 group"
              style={chamferClip}
            >
              <ArrowLeft size={16} className="text-[#ff2a2a] group-hover:-translate-x-1 transition-transform" />
              <span className="hidden sm:inline font-bold tracking-wider text-sm">返回</span>
            </button>
          )}

          {/* 标题区域 */}
          <div className="flex flex-col">
            <div className="flex items-center gap-3">
              <h1 className="text-xl sm:text-2xl font-bold text-white tracking-wider">{title}</h1>
              <span className={`text-[10px] font-bold px-1.5 py-0.5 rounded-[1px] ${
                isConfigured ? 'bg-green-500 text-black' : 'bg-[#ff2a2a] text-black'
              }`}>
                {isConfigured ? 'CONNECTED' : 'OFFLINE'}
              </span>
            </div>
            {subtitle && (
              <span className="hidden sm:block text-[10px] font-mono text-gray-600 tracking-[0.2em] mt-[-2px]">
                {subtitle}
              </span>
            )}
          </div>
        </div>

        {/* 右侧：功能按钮组 */}
        <div className="flex items-center gap-2 sm:gap-3">
          
          {/* 帮助按钮 */}
          {onHelp && (
            <button 
              onClick={onHelp}
              className="w-10 h-10 flex items-center justify-center bg-[#050505] border border-[#222] text-gray-300 hover:text-white hover:border-white/50 transition-colors"
              style={chamferClip}
              title="帮助说明"
            >
              <FileText size={18} />
            </button>
          )}

          {/* 映射按钮 */}
          {onMapping && (
            <button 
              onClick={onMapping}
              className={`w-10 h-10 flex items-center justify-center bg-[#050505] border text-gray-300 hover:text-white hover:border-white/50 transition-colors ${
                isConfigured ? 'border-green-500/50' : 'border-[#222]'
              }`}
              style={chamferClip}
              title="数据映射"
            >
              <Link size={18} />
            </button>
          )}

          {/* 设置按钮 */}
          {onSettings && (
            <button 
              onClick={onSettings}
              className="w-10 h-10 flex items-center justify-center bg-[#050505] border border-[#222] text-gray-300 hover:text-white hover:border-white/50 transition-colors"
              style={chamferClip}
              title="设置"
            >
              <Settings size={18} />
            </button>
          )}

          {/* 用户按钮 (主强调) */}
          <button 
            className="w-10 h-10 flex items-center justify-center bg-[#ff2a2a] text-black hover:bg-white transition-colors shadow-[0_0_10px_rgba(255,0,60,0.4)]"
            style={chamferClip}
            title="用户"
          >
            <User size={20} strokeWidth={2.5} />
          </button>
        </div>
      </div>
    </div>
  );
};
```

---

## � 多语言切换规范 (Language Switcher)

### 设计规范

用于支持多语言的组件，提供语言按钮和选择弹窗。

#### 核心参数

| 属性 | 值 |
|------|-----|
| **语言按钮** | 与顶部栏按钮统一样式 (40x40px, 切角) |
| **按钮图标** | `Globe` 图标 (lucide-react) |
| **弹窗背景** | `bg-black/80` + `backdrop-blur-sm` |
| **列表项** | 悬停高亮，当前语言红色背景标记 |

### 类型定义

```tsx
// 多语言表字段映射
interface LanguageFieldMapping {
  sheetName: string;      // 多语言工作表名称
  startRow: number;       // 数据起始行（1-indexed）
  idColumn: string;       // ID 列 (第1列)
  langColumns: string[];  // 语言列 (第3列起)
}

// 多语言数据存储 Map: ID -> { langName: text }
type LanguageData = Map<string, { [lang: string]: string }>;
```

### 完整代码模板

```tsx
import React, { useState } from 'react';
import { Globe, X } from 'lucide-react';

interface LanguageSwitcherProps {
  availableLanguages: string[];
  currentLanguage: string;
  onLanguageChange: (lang: string) => void;
}

export const LanguageSwitcher: React.FC<LanguageSwitcherProps> = ({
  availableLanguages,
  currentLanguage,
  onLanguageChange,
}) => {
  const [isOpen, setIsOpen] = useState(false);

  const chamferClip = {
    clipPath: 'polygon(8px 0, 100% 0, 100% calc(100% - 8px), calc(100% - 8px) 100%, 0 100%, 0 8px)'
  };

  if (availableLanguages.length <= 1) return null;

  return (
    <>
      {/* 语言按钮 */}
      <button 
        onClick={() => setIsOpen(true)}
        className="w-10 h-10 flex items-center justify-center bg-[#050505] border border-[#222] text-gray-300 hover:text-white hover:border-white/50 transition-colors"
        style={chamferClip}
        title="切换语言"
      >
        <Globe size={18} />
      </button>

      {/* 语言选择弹窗 */}
      {isOpen && (
        <div 
          className="fixed inset-0 z-[100] flex items-center justify-center bg-black/80 backdrop-blur-sm"
          onClick={() => setIsOpen(false)}
        >
          <div 
            className="bg-[#0e0e0e] border border-white/10 w-[300px] max-h-[60vh] overflow-hidden"
            onClick={(e) => e.stopPropagation()}
          >
            {/* 头部 */}
            <div className="h-12 flex items-center justify-between px-4 border-b border-white/10 bg-[#121212]">
              <div className="flex items-center gap-2">
                <Globe size={16} className="text-[#ff2a2a]" />
                <span className="text-white font-bold tracking-wider">选择语言</span>
              </div>
              <button onClick={() => setIsOpen(false)} className="text-gray-500 hover:text-white">
                <X size={18} />
              </button>
            </div>

            {/* 语言列表 */}
            <div className="p-2 overflow-y-auto max-h-[50vh]">
              {availableLanguages.map((lang) => (
                <button
                  key={lang}
                  onClick={() => {
                    onLanguageChange(lang);
                    setIsOpen(false);
                  }}
                  className={`
                    w-full text-left px-4 py-3 transition-colors
                    ${currentLanguage === lang 
                      ? 'bg-[#ff2a2a] text-black font-bold' 
                      : 'text-gray-300 hover:bg-white/10 hover:text-white'
                    }
                  `}
                >
                  {lang}
                </button>
              ))}
            </div>
          </div>
        </div>
      )}
    </>
  );
};

// 从多语言数据中获取文本的工具函数
export function getText(
  id: string | undefined, 
  langData: LanguageData, 
  currentLang: string
): string {
  if (!id) return '';
  const entry = langData.get(id);
  if (!entry) return id;
  return entry[currentLang] || entry['简中'] || Object.values(entry)[0] || id;
}
```

---

## �🃏 Item 卡片规范 (ItemCard)

### 设计规范

```
┌─────────────────────────────┐
│ ITEM_01                   ◤▌│  顶部：ID (#3B3B3B) + 品质三角(16px)
│                              │
│          [图标 36px]         │  中央：图标 (默认 #3B3B3B, hover scale-110)
│                              │
├──────────────────────────────┤
│ 名称              x99        │  底部栏 (28px)：名称 + 数量
└──────────────────────────────┘
```

#### 核心参数

| 属性 | 值 |
|------|-----|
| **尺寸** | 100 x 100 px (固定) |
| **背景** | `#0a0a0a` |
| **边框** | `1px solid #333` |
| **品质角标** | 16px border 三角形 (右上角，透明度 35%) |
| **图标尺寸** | 36px (固定) |
| **图标偏移** | 向下 5px (`pt-[5px]`) |
| **默认颜色** | `#3B3B3B` (图标和文本) |
| **底部信息栏** | 28px 高度，`bg-black` 背景 |
| **Hover效果** | 图标 `scale-110`，边框 `border-white/20` |

#### 品质颜色

```css
/* 稀有度颜色映射 */
COMMON    →  #16a34a  /* 绿色 */
RARE      →  #60a5fa  /* 蓝色 */
EPIC      →  #a855f7  /* 紫色 */
LEGENDARY →  #fbbf24  /* 金色 */
```

### 完整代码模板

```tsx
import React from 'react';
import { Box } from 'lucide-react';

interface ItemCardProps {
  id: string;
  name: string;
  rarity: 'COMMON' | 'RARE' | 'EPIC' | 'LEGENDARY';
  quantity?: number;
  claimed?: boolean;
}

// 根据稀有度获取颜色
const getRarityColor = (rarity: string) => {
  switch (rarity) {
    case 'LEGENDARY': return '#fbbf24';
    case 'EPIC': return '#a855f7';
    case 'RARE': return '#60a5fa';
    default: return '#16a34a';
  }
};

export const ItemCard: React.FC<ItemCardProps> = ({
  id,
  name,
  rarity,
  quantity = 0,
  claimed = false,
}) => {
  const rarityColor = getRarityColor(rarity);

  return (
    <div className="relative w-[100px] h-[100px] bg-[#0a0a0a] border border-[#333] flex flex-col group overflow-hidden select-none shrink-0 shadow-lg">
      
      {/* 顶部 ID 标签 */}
      <div className="absolute top-1 left-2 text-[10px] font-mono text-[#333] z-10 tracking-wider font-bold">
        ITEM_{id.toUpperCase().substring(0, 2)}
      </div>

      {/* 品质三角角标 (16px, 透明度 35%) */}
      <div 
        className="absolute top-[2px] right-[2px] w-0 h-0 border-l-[16px] border-l-transparent border-t-[16px] z-20 opacity-35"
        style={{ borderTopColor: rarityColor }}
      />

      {/* 中央图标 (36px, 默认颜色 #3B3B3B, 向下偏移5px) */}
      <div className="flex-1 flex items-center justify-center relative z-10 pt-[5px]">
        <Box 
          className="text-[#3B3B3B] group-hover:scale-110 transition-transform duration-200" 
          size={36} 
          strokeWidth={1.5}
        />
      </div>

      {/* 底部信息栏 (28px, 默认颜色 #3B3B3B) */}
      <div className="h-[28px] bg-black w-full flex items-center px-1.5 justify-between border-t border-[#222] relative z-20">
        <span className="text-[#3B3B3B] font-bold text-sm font-sans truncate flex-1">{name}</span>
        <span className="text-[#3B3B3B] font-mono text-sm tracking-tighter ml-1 opacity-90">x{quantity}</span>
      </div>

      {/* 已领取覆盖层 (可选) */}
      {claimed && (
        <div className="absolute inset-0 bg-black/80 z-30 flex items-center justify-center backdrop-blur-[1px]">
          <div className="border border-gray-600 bg-black/90 px-2 py-0.5 transform -rotate-12 shadow-lg">
            <span className="text-gray-400 text-[10px] font-bold uppercase tracking-wider">已领取</span>
          </div>
        </div>
      )}
      
      {/* Hover 边框效果 */}
      <div className="absolute inset-0 border border-transparent group-hover:border-white/20 pointer-events-none transition-colors duration-300" />
    </div>
  );
};
```


---

## 📋 列表项规范 (ListItem)

### 设计规范

用于邮件列表、任务列表等场景。

```
┌──────────────────────────────────────────────────────────────┐
│ ┃  [图标]  标题                                    剩余 7 天 ●│
│ ┃  [类型]  发送者/副标题                                      │
└──────────────────────────────────────────────────────────────┘
```

### 完整代码模板

```tsx
import React from 'react';
import { Info, Gift, Trophy, ShieldAlert } from 'lucide-react';

interface ListItemProps {
  id: string;
  title: string;
  subtitle?: string;
  type: 'system' | 'event' | 'arena' | 'compensation';
  badge?: string;
  isRead?: boolean;
  isSelected?: boolean;
  onClick?: () => void;
}

// 类型样式映射
const typeStyles = {
  system: { border: 'border-blue-500', borderDim: 'border-blue-900/50', icon: <Info size={20} className="text-blue-500" />, label: '系统' },
  event: { border: 'border-purple-500', borderDim: 'border-purple-900/50', icon: <Gift size={20} className="text-purple-500" />, label: '活动' },
  arena: { border: 'border-[#ff2a2a]', borderDim: 'border-red-900/50', icon: <Trophy size={20} className="text-[#ff2a2a]" />, label: '竞技' },
  compensation: { border: 'border-orange-500', borderDim: 'border-orange-900/50', icon: <ShieldAlert size={20} className="text-orange-500" />, label: '补偿' },
};

export const ListItem: React.FC<ListItemProps> = ({
  id,
  title,
  subtitle,
  type,
  badge,
  isRead = true,
  isSelected = false,
  onClick,
}) => {
  const styles = typeStyles[type];

  return (
    <div
      onClick={onClick}
      className={`
        relative w-full p-3 cursor-pointer transition-all duration-200 group
        border-l-[6px] bg-gradient-to-r
        ${isSelected ? 'bg-white/10' : 'bg-black/40 hover:bg-white/5'}
        ${isSelected ? styles.border : `${styles.borderDim} hover:${styles.border}`}
      `}
    >
      <div className="flex items-start gap-3 relative z-10">
        {/* 类型图标 */}
        <div className="flex flex-col items-center justify-center gap-1 mt-0.5 min-w-[30px]">
          {styles.icon}
          <span className="text-[10px] text-gray-500 uppercase font-mono tracking-tighter scale-90">
            {styles.label}
          </span>
        </div>

        {/* 文本内容 */}
        <div className="flex-1 min-w-0 flex flex-col justify-center">
          <h3 className={`font-bold text-base truncate leading-tight transition-colors ${
            isSelected ? 'text-white' : 'text-gray-300 group-hover:text-white'
          }`}>
            {title}
          </h3>
          <div className="flex justify-between items-center mt-1">
            <span className="text-xs text-gray-500 truncate max-w-[60%]">{subtitle}</span>
            <div className={`text-[10px] font-mono flex items-center gap-2 ${
              isSelected ? 'text-white/80' : 'text-gray-500'
            }`}>
              {badge && <span>{badge}</span>}
              {!isRead && (
                <div className="w-2 h-2 rounded-full bg-[#ff2a2a] animate-pulse" />
              )}
            </div>
          </div>
        </div>
      </div>

      {/* 选中高亮覆盖层 */}
      {isSelected && (
        <div className="absolute inset-0 bg-white/5 pointer-events-none mix-blend-overlay" />
      )}
      
      {/* 分隔线 */}
      <div className="absolute bottom-0 right-0 w-[90%] h-[1px] bg-white/5" />
    </div>
  );
};
```

---

## 🎛️ 侧边栏规范 (Sidebar)

### 设计规范

```
┌────────────────────┐
│ │ MISSION LOG      │
│ │ SYS.TASK.V.9.2   │
│ │                  │
│ ┃ 📅 每日任务    → │  活动项
│ │ 📆 每周任务      │
│ │                  │
│ │                  │
│ │ SYNC: AUTO       │  底部状态
│ │ SERVER: ASIA-01  │
└────────────────────┘
```

### 完整代码模板

```tsx
import React from 'react';
import { ChevronRight, LucideIcon } from 'lucide-react';

interface SidebarItem {
  id: string;
  label: string;
  icon: React.ReactNode;
}

interface SidebarProps {
  title: string;
  subtitle?: string;
  items: SidebarItem[];
  activeId: string;
  onSelect: (id: string) => void;
}

export const Sidebar: React.FC<SidebarProps> = ({
  title,
  subtitle,
  items,
  activeId,
  onSelect,
}) => {
  return (
    <div className="min-w-[180px] max-w-[240px] w-[15vw] h-full flex flex-col pt-6 relative z-10 bg-[#050505] shrink-0">
      
      {/* 装饰线 */}
      <div className="absolute left-6 top-0 bottom-0 w-px bg-white/10" />

      {/* 标题 */}
      <div className="mb-8 pl-6 pr-3">
        <h1 className="text-lg lg:text-xl font-bold tracking-wider text-white uppercase">{title}</h1>
        {subtitle && (
          <div className="text-[9px] text-[#ff2a2a] font-mono tracking-[0.15em] mt-1 hidden md:block">
            {subtitle}
          </div>
        )}
      </div>

      {/* 菜单项 */}
      <div className="flex flex-col space-y-4">
        {items.map((item) => {
          const isActive = activeId === item.id;
          return (
            <button
              key={item.id}
              onClick={() => onSelect(item.id)}
              className={`
                group relative h-12 lg:h-14 pl-6 pr-3 flex items-center 
                transition-all duration-300 w-full text-left
                ${isActive ? 'text-white bg-white/5' : 'text-gray-500 hover:text-gray-300'}
              `}
            >
              {/* 活动指示器 */}
              {isActive ? (
                <div className="absolute left-0 top-0 bottom-0 w-1 bg-[#ff2a2a] shadow-[0_0_10px_#ff2a2a]" />
              ) : (
                <div className="absolute left-0 top-1/2 -translate-y-1/2 w-1 h-1 bg-gray-700" />
              )}

              {/* 图标和标签 */}
              <span className={`mr-4 transition-colors ${isActive ? 'text-[#ff2a2a]' : ''}`}>
                {item.icon}
              </span>
              <span className="font-bold tracking-wider text-sm lg:text-base">
                {item.label}
              </span>

              {/* 箭头指示器 */}
              {isActive && (
                <ChevronRight className="ml-auto text-[#ff2a2a] animate-pulse" size={16} />
              )}
            </button>
          );
        })}
      </div>

      {/* 底部状态 */}
      <div className="mt-auto pb-6 pl-6 text-[10px] text-gray-600 font-mono hidden lg:block">
        <p>SYNC: AUTO</p>
        <p>SERVER: ASIA-01</p>
      </div>
    </div>
  );
};
```

---

## 🔘 按钮规范 (CyberButton)

### 完整代码模板

```tsx
import React from 'react';

interface CyberButtonProps {
  variant?: 'primary' | 'secondary' | 'danger' | 'ghost';
  children: React.ReactNode;
  onClick?: () => void;
  disabled?: boolean;
  className?: string;
}

export const CyberButton: React.FC<CyberButtonProps> = ({
  variant = 'primary',
  children,
  onClick,
  disabled = false,
  className = '',
}) => {
  const chamferClip = {
    clipPath: 'polygon(8px 0, 100% 0, 100% calc(100% - 8px), calc(100% - 8px) 100%, 0 100%, 0 8px)'
  };

  const variants = {
    primary: 'bg-[#ff2a2a] hover:bg-[#ff4444] text-black font-bold shadow-[0_0_10px_rgba(255,0,60,0.4)]',
    secondary: 'bg-[#0a0a0a] border border-[#333] text-white hover:bg-[#151515] hover:border-white/50',
    danger: 'bg-red-600 hover:bg-red-700 text-white font-bold',
    ghost: 'bg-transparent border border-white/10 text-gray-400 hover:text-white hover:bg-white/5',
  };

  return (
    <button
      onClick={onClick}
      disabled={disabled}
      style={chamferClip}
      className={`
        px-6 py-3 tracking-wider transition-all flex items-center justify-center gap-2
        disabled:opacity-50 disabled:cursor-not-allowed
        ${variants[variant]}
        ${className}
      `}
    >
      {children}
    </button>
  );
};
```

---

## 📦 模态框规范 (Modal)

### 完整代码模板

```tsx
import React from 'react';
import { X } from 'lucide-react';

interface ModalProps {
  isOpen: boolean;
  onClose: () => void;
  title: string;
  subtitle?: string;
  children: React.ReactNode;
  footer?: React.ReactNode;
  maxWidth?: string;
}

export const Modal: React.FC<ModalProps> = ({
  isOpen,
  onClose,
  title,
  subtitle,
  children,
  footer,
  maxWidth = '850px',
}) => {
  if (!isOpen) return null;

  return (
    <div className="fixed inset-0 z-[100] flex items-center justify-center bg-black/80 backdrop-blur-sm animate-fade-in">
      <div 
        className="bg-[#0e0e0e] border border-white/10 flex flex-col relative shadow-2xl shadow-black/80 max-h-[90vh]"
        style={{ width: maxWidth }}
      >
        {/* 头部 */}
        <div className="h-14 flex items-center justify-between px-6 border-b border-white/10 bg-[#121212] shrink-0">
          <div className="flex items-center gap-3">
            <div className="w-1 h-5 bg-[#ff2a2a]" />
            <h2 className="text-xl font-bold text-white tracking-wider">{title}</h2>
            {subtitle && (
              <span className="text-[10px] text-gray-500 font-mono tracking-widest">{subtitle}</span>
            )}
          </div>
          <button onClick={onClose} className="text-gray-500 hover:text-white transition-colors">
            <X size={24} />
          </button>
        </div>

        {/* 内容区 */}
        <div className="p-8 overflow-y-auto flex-1">
          {children}
        </div>

        {/* 底部 */}
        {footer && (
          <div className="h-20 border-t border-white/10 flex items-center justify-end px-8 gap-4 bg-[#121212] shrink-0">
            {footer}
          </div>
        )}
      </div>
    </div>
  );
};
```

---

## � 材料获得弹窗规范 (RewardModal)

### 设计规范

用于展示任务奖励、抽奖结果、邮件附件领取等场景的全屏弹窗。

```
┌─────────────────────────────────────────────────────────────────────────┐
│                                   ○ ○ ○                                 │  背景：旋转装饰圆环
│                         < ─────────────── >                             │
│                                                                         │
│                           「 获得奖励 」                                 │  标题：带装饰括号
│                                                                         │
│                        ┌─────┐  ┌─────┐  ┌─────┐                        │
│                        │Item1│  │Item2│  │Item3│                        │  物品网格：居中排列
│                        └─────┘  └─────┘  └─────┘                        │
│                                                                         │
│                          - 点击空白处关闭 -                              │  底部提示
└─────────────────────────────────────────────────────────────────────────┘
```

#### 核心参数

| 属性 | 值 |
|------|-----|
| **背景** | `bg-black/80` + `backdrop-blur-sm` |
| **装饰圆环** | 800px、600px、900px 三层，慢旋转动画 |
| **标题样式** | 4xl 字体，`tracking-[0.2em]`，红光阴影 |
| **物品间距** | `gap-8` (32px) |
| **入场动画** | 标题从上滑入，物品依次缩放淡入 |
| **关闭方式** | 点击空白处关闭 |

### 完整代码模板

```tsx
import React, { useEffect, useState } from 'react';
import { ChevronRight, ChevronLeft } from 'lucide-react';

interface RewardItem {
  id: string;
  name: string;
  quantity: number;
  rarity: string;
}

interface RewardModalProps {
  isOpen: boolean;
  items: RewardItem[];
  onClose: () => void;
}

export const RewardModal: React.FC<RewardModalProps> = ({ isOpen, items, onClose }) => {
  const [showContent, setShowContent] = useState(false);

  useEffect(() => {
    if (isOpen) {
      const timer = setTimeout(() => setShowContent(true), 100);
      return () => clearTimeout(timer);
    } else {
      setShowContent(false);
    }
  }, [isOpen]);

  if (!isOpen) return null;

  return (
    <div 
      className="fixed inset-0 z-[100] flex items-center justify-center bg-black/80 backdrop-blur-sm transition-opacity duration-300"
      onClick={onClose}
    >
      {/* 背景装饰圆环 */}
      <div className={`absolute inset-0 overflow-hidden pointer-events-none transition-all duration-700 ${showContent ? 'opacity-100 scale-100' : 'opacity-0 scale-90'}`}>
        <div className="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 w-[800px] h-[800px] border border-white/5 rounded-full" />
        <div className="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 w-[600px] h-[600px] border border-white/10 rounded-full border-dashed animate-[spin_60s_linear_infinite]" />
        <div className="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 w-[900px] h-[900px] border border-[#ff2a2a]/5 rounded-full" />
      </div>

      <div 
        className={`relative flex flex-col items-center max-w-5xl w-full p-10 transition-all duration-500 transform ${showContent ? 'translate-y-0 opacity-100' : 'translate-y-10 opacity-0'}`}
        onClick={(e) => e.stopPropagation()}
      >
        
        {/* 标题区域：带装饰括号 */}
        <div className="flex items-center gap-6 mb-16">
          <div className="flex items-center text-[#ff2a2a] opacity-80">
            <span className="w-12 h-[1px] bg-gradient-to-r from-transparent to-[#ff2a2a]" />
            <ChevronLeft size={24} />
          </div>
          
          <h2 className="text-4xl font-bold text-white tracking-[0.2em] drop-shadow-[0_0_15px_rgba(255,0,60,0.6)]">
            获得奖励
          </h2>
          
          <div className="flex items-center text-[#ff2a2a] opacity-80">
            <ChevronRight size={24} />
            <span className="w-12 h-[1px] bg-gradient-to-l from-transparent to-[#ff2a2a]" />
          </div>
        </div>

        {/* 中央发光效果 */}
        <div className="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 w-64 h-64 bg-[#ff2a2a] blur-[100px] opacity-10 pointer-events-none" />

        {/* 物品网格 */}
        <div className="flex flex-wrap justify-center gap-8 z-10">
          {items.map((item, index) => (
            <div 
              key={index} 
              className="transition-all duration-500 hover:scale-105"
              style={{ 
                opacity: showContent ? 1 : 0, 
                transform: showContent ? 'scale(1)' : 'scale(0.5)',
                transitionDelay: `${index * 150 + 200}ms` 
              }}
            >
              {/* 使用 ItemCard 组件 */}
              <ItemCard item={item} />
            </div>
          ))}
        </div>

        {/* 底部关闭提示 */}
        <div 
          className={`mt-16 text-gray-500 font-mono text-xs tracking-[0.5em] animate-pulse transition-all duration-1000 ${showContent ? 'opacity-100' : 'opacity-0'}`}
          style={{ transitionDelay: '800ms' }}
        >
          - 点击空白处关闭 -
        </div>
      </div>
    </div>
  );
};
```

---

## �🎬 动画效果

### 在 index.css 中定义

```css
/* 淡入动画 */
@keyframes fade-in {
  from { opacity: 0; }
  to { opacity: 1; }
}

.animate-fade-in {
  animation: fade-in 0.2s ease-out;
}

/* 从上淡入 */
@keyframes fade-in-down {
  0% {
    opacity: 0;
    transform: translateX(-50%) translateY(-20px);
  }
  100% {
    opacity: 1;
    transform: translateX(-50%) translateY(0);
  }
}

.animate-fade-in-down {
  animation: fade-in-down 0.3s ease-out;
}

/* 滑入动画 */
@keyframes slide-in {
  from {
    opacity: 0;
    transform: translateX(20px);
  }
  to {
    opacity: 1;
    transform: translateX(0);
  }
}

.animate-slide-in {
  animation: slide-in 0.3s ease-out;
}

/* 故障效果 */
@keyframes glitch {
  2%, 64% { transform: translate(2px, 0) skew(0deg); }
  4%, 60% { transform: translate(-2px, 0) skew(0deg); }
  62% { transform: translate(0, 0) skew(5deg); }
}

.animate-glitch {
  animation: glitch 1s linear infinite;
}

/* 扫描线效果 */
@keyframes scan {
  0% { top: 0%; }
  100% { top: 100%; }
}

.animate-scan {
  animation: scan 2s linear infinite;
}
```

---

## ✅ UI 检查清单

- [ ] 使用统一的 Cyberpunk 颜色系统
- [ ] 按钮和交互元素使用切角样式
- [ ] 顶部栏包含装饰条和功能按钮组
- [ ] 卡片组件包含品质指示器和选中效果
- [ ] 列表项包含类型图标和左侧边框
- [ ] 模态框使用统一的头部和底部样式
- [ ] 包含必要的动画效果 (fade-in, pulse 等)
- [ ] 滚动条使用自定义样式

